# Usage

```
pentrop 0.1.0

USAGE:
    pentrop [OPTIONS] <INPUT>

ARGS:
    <INPUT>

OPTIONS:
    -h, --help                     Print help information
    -t, --threshold <THRESHOLD>    If specified the program will fail for inputs that do not exceed
                                   the threshold
    -V, --version                  Print version information
```
